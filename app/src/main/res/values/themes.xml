<resources>
    <!-- Base application theme. -->
    <style name="Theme.TimeIsNow" parent="Theme.MaterialComponents.NoActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/darkAccentColor</item>
        <item name="colorAccent">@color/accentColor</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorBackground">#8D8F97</item>
    </style>

    <!-- Menu Style -->
    <style name="navigationTheme">
        <!-- Color of text and icon when SELECTED -->
        <item name="colorPrimary">@color/accentColor</item>
        <!-- Background color when Selected -->
        <item name="colorControlHighlight">@color/black</item>

    </style>

</resources>